def encrypt(string, key):
    return string[chr(ord(character) + key) for character in string] 

def main():
    N = input()
    S = raw_input()
    K = input()

    print encrypt(S, K)

if __name__ == '__main__':
    main()
