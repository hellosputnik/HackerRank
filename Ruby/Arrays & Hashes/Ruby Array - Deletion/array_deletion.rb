def end_arr_delete(array)
    array.pop();
end

def start_arr_delete(array)
    array.shift();
end

def delete_at_arr(array, index)
    array.delete_at(index);
end

def delete_all(array, value)
    array.delete(value);
end

