import collections
import operator

def main():
    counter = collections.Counter(raw_input())
    common = sorted(counter.items(), key=operator.itemgetter(1))[:3]
    for c in common:
        print c[0], c[1]

if __name__ == '__main__':
    main()

